import React from "react";
import { StyleSheet, View } from "react-native";

const EndFlatList = () => {
  return <View style={styles.endFlatList}></View>;
};

const styles = StyleSheet.create({
  endFlatList: {
    height: 20 + 70 + 20,
  },
});

export default EndFlatList;
