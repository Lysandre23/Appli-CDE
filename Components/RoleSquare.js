import React from "react";
import {View, StyleSheet, TouchableOpacity} from "react-native";

const RoleSquare = (props) => {
    return(
        <View style={{width: 20, height: 20, backgroundColor: props.color, borderRadius: 3}}>

        </View>
    )
}

const styles = StyleSheet.create({
    main: {

    }
});

export default RoleSquare;